#include <stdio.h>

/**
 * main - Prints the lower case alphabet in reverse
 * followed by a new line
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
char c = 'a';

while
(c <= 'z');
{
putchar(c);
c++;
}
putchar('\n');

return (0);
}
