#include "main.h"

/**
 * main - Multiplies two integers
 * the first integer to multiply is a
 * the second integer to multiply with a is b
 * Return the result of the multiplication of the two integers
 */

int mul(int a, int b)
{
return (a * b);
}
