#include "main.h"

/**
 * main - checks for uppercase character
 * Returns 1 if c is uppercase
 * Returns 0 other wise
 */

int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
return ('\n')
}
else
{
return (0);
}
}
