#include "main.h"

/**
 * main - tests the function positive_or_negative()
 * must give correct output when given a case of 0
 *
 * Return: 0
 */

int main(void)
{
int i;

i = 98;
positive_or_negative(i);

return (0);
}
