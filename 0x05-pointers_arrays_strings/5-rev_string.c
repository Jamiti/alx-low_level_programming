#include "main.h"

/**
 * main - reverses a string
 *
 * Return: 0
 */

void rev_string(char *s)
{
int i, length1, length2;
char t;

length1 = 0;
length2 = 0;

while (s[length1] != '\0')
length1++

length2 = length1 - 1

}
